# Native Gameplay

`GameState` is the deterministic authority for the current Ares Reach graybox. It owns fixed-step movement, mission bounds, objective completion, reset behavior, camera derivation, and the renderer-facing scene instance set.
