# Native Renderer

The Direct3D 12 renderer consumes `RenderScene` snapshots from native gameplay. It renders independently transformed cube primitives with per-instance tint, depth testing, directional lighting, resize-safe frame resources, validation-enabled WARP execution, and optional GPU back-buffer readback.
