# Native Tests

`game_state_tests.cpp` verifies deterministic movement, mission completion, post-completion arrest, reset behavior, and the complete Ares Reach graybox scene instance set.

The tests are built with `/W4 /WX` and executed through CTest in Windows CI.
