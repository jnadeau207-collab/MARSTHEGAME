name: Format Phase 1 durability

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install Ruff
        run: python -m pip install -r requirements-dev.txt
      - name: Apply pinned formatter
        run: >-
          ruff format
          game/core/checkpoint.py
          game/core/diagnostics.py
          game/core/engine.py
          game/core/save.py
          tools/phase1_quality_audit.py
          tests/test_diagnostics.py
          tests/test_save_progression.py
      - name: Capture exact patch
        run: |
          mkdir -p build
          git diff --binary > build/phase1-durability-format.patch
      - uses: actions/upload-artifact@v4
        with:
          name: formatted-phase1-durability-${{ github.sha }}
          path: |
            build/phase1-durability-format.patch
            game/core/checkpoint.py
            game/core/diagnostics.py
            game/core/engine.py
            game/core/save.py
            tools/phase1_quality_audit.py
            tests/test_diagnostics.py
            tests/test_save_progression.py
          if-no-files-found: error
